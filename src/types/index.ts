export type TUser = {
    id: string;
    name: string;
    email: string;
    passowd: string;
    createdAT: string;
}

export type TProduct = {
    id: string;
    name: string;
    price: number;
    description: string;
    imageUrl: string;
}